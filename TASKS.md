# 📋 Журнал завдань ShopCase (Task Log)

> **Правило ведення:** Цей документ оновлюється на початку кожного нового завдання та після його завершення. Фіксуються всі виконані кроки, поточний фокус та наступні плани.

---

## 🟢 Поточний стан проекту (Status: Live & Functional)
- **Сайт:** [shopcase.top](https://shopcase.top)
- **Платформа:** PHP 8.1+ (OOP) + SQLite / Flat-JSON + Vanilla ES6 JS + CSS Grid/Flexbox
- **Інтеграції:** EndorPhone API (залишки, авто-замовлення, статуси), Нова Пошта API (міста та відділення), Онлайн 3D-конструктор з власним фото

---

## 📌 Активні завдання (In Progress ⏳)
*Усі завдання Етапів 6–7 (Telegram-бот, SEO-оптимізація, ЧПУ, Sitemap XML, Robots.txt, Тестування замовлень) успішно завершено!*

---

## ✅ Виконані завдання (Done)

### Етап 1: Базова інфраструктура та архітектура
- [x] **Аудит середовища хостингу:** Налаштування під PHP 8.1, Apache + Nginx reverse proxy, підтримка cURL, SQLite, JSON.
- [x] **Модульна архітектура проекту:**
  - `app/` (OOP-сервіси: `Config.php`, `Database.php`, `EndorPhone.php`, `CatalogService.php`, `StockService.php`, `NovaPoshta.php`, `OrderService.php`).
  - `views/` (компонентні шаблони: `layout/`, `components/`).
  - `css/` та `js/` (модульні стилі та ES6 скрипти без важких фреймворків).
  - `data/` (захищена база SQLite `shopcase.sqlite` + JSON кеш залишків).
- [x] **Безпека:** Налаштовано правила `.htaccess` для захисту `.env`, `app/`, `views/`, `data/`, системних файлів (403 Forbidden).

### Етап 2: Каталог та вітрина
- [x] **Наповнення каталогу дизайнів:** 238 авторських макетів у 22 категоріях із прев'ю-зображеннями та метаданими.
- [x] **Пошук та фільтрація:** Миттєва фільтрація за категоріями, текстовий пошук принтів, пагінація сторінок (24 принти на сторінку).
- [x] **Hero-секція та банери:** Сучасний адаптивний перший екран, промо-переваги та блок "Як це працює".

### Етап 3: Інтеграція з EndorPhone API та Кастомізатор
- [x] **Синхронізація залишків (Stock):** Отримання та кешування наявності матеріалів для 995 моделей (678 активних у наявності).
- [x] **Інтерактивний кастомізатор (Customizer Modal):**
  - Вибір смартфону з миттєвим "живим" пошуком серед 678 моделей.
  - Динамічний вибір матеріалу (Силікон, TPU, Матовий, 3D пластик, MagSafe тощо) із підтягуванням актуальної ціни.
  - Автоматичне формування валідного артикулу EndorPhone: `{design_id}{mat_code}-{model_id}`.
- [x] **Онлайн 3D-конструктор з власним фото:**
  - Банер-заклик на головній сторінці.
  - Модальне вікно з інтегрованим iframe-конструктором EndorPhone drop-партнера.
  - Кнопка відкриття конструктора на повний екран у новій вкладці.

### Етап 4: Оформлення замовлення та Доставка
- [x] **Форма замовлення (Order Modal):** Валідація ПІБ, телефону (`+380...`), вибір способу оплати.
- [x] **Інтеграція Нової Пошти:** Живий пошук населених пунктів та відділень/поштоматів через API.
- [x] **Автоматична передача замовлень в EndorPhone API:** Створення замовлення в системі постачальника та збереження в локальну базу даних.
- [x] **Модуль відстеження замовлень (Tracking Modal):** Пошук історії та статусів замовлень за номером телефону клієнта.
- [x] **Автоматичний Cron-скрипт (`cron.php`):** Фонове оновлення залишків товарів та перевірка статусів активних замовлень.

### Етап 5: Повний аудит та підтримка всіх методів EndorPhone API (згідно з `api.png`)
- [x] **Аналіз та документування специфікації:** Створено вичерпну документацію [`ENDORPHONE_API.md`](file:///j:/site/SC/ENDORPHONE_API.md) на основі файлу `api.png`.
- [x] **Виправлення та відповідність полів замовлення:** Виправлено поле `prepayment_type` у `OrderService.php`, забезпечено валідну транслітерацію латиницею (захист від помилки `#1001`).
- [x] **Розширення клієнта EndorPhone (`app/EndorPhone.php`):**
  - Реалізовано метод `getOrderStatuses()` (`/1.0/getinfo?option=orderStatus`) для отримання системних статусів.
  - Реалізовано метод `getOrdersList()` (`/1.0/getorderslist`) для вибірки замовлень за ID, датами чи статусом.
  - Реалізовано метод `getPayouts()` (`/1.0/payouts`) для фінансової звітності по виплатах.
  - Реалізовано метод `getStatements()` (`/1.0/statements`) для відстеження транзакцій/балансу.
  - Реалізовано метод `getStockByProducts()` (`/1.0/stock`) для перевірки наявності окремих артикулів.
- [x] **Словник статусів та бейджів:** Додано константу `Config::ENDORPHONE_STATUSES` (20 офіційних статусів постачальника).
- [x] **Автоматичне підтягування ТТН та статусів:** `cron.php` та `tracking.js` тепер оновлюють та відображають актуальні трекінг-номери Нової Пошти з прямим посиланням на відстеження.

### Етап 6: Нова Пошта Ref-інтеграція, Telegram-сповіщення та Адмін-панель
- [x] **Повна інтеграція Nova Poshta API:**
  - Оновлено [`app/NovaPoshta.php`](file:///j:/site/SC/app/NovaPoshta.php): живий пошук міст та вивантаження відділень/поштоматів (`getWarehouses`) з кешуванням 24 години.
  - Вбудовано базу з UUID `CityRef` для міст України.
  - Оновлено форму замовлення [`js/order.js`](file:///j:/site/SC/js/order.js): автопідвантаження та випадаючий список відділень при виборі міста.
  - Оновлено [`app/OrderService.php`](file:///j:/site/SC/app/OrderService.php): передача точних `CityRef` та `WarehouseRef` у замовлення EndorPhone API для безпомилкової генерації ТТН.
- [x] **Сервіс Telegram-сповіщень ([`app/TelegramService.php`](file:///j:/site/SC/app/TelegramService.php)):**
  - Миттєве сповіщення адміністратора в Telegram при новому замовленні (покупець, телефон, артикул, сума, адреса доставки).
  - Автоматичне сповіщення у [`cron.php`](file:///j:/site/SC/cron.php) при появі нового номера ТТН або зміні статусу замовлення постачальником.
- [x] **Адмін-панель ([`admin.php`](file:///j:/site/SC/admin.php)):**
  - Захист входу за паролем (налаштовується в `.env`).
  - Статистика замовлень, загального доходу, наявних моделей телефонів.
  - Таблиця замовлень з повними даними, статусами EndorPhone та трекінг-посиланнями Нової Пошти.
  - Вкладка фінансових звітів з EndorPhone API (`/1.0/payouts` та `/1.0/statements`).
  - Інструменти для ручного оновлення залишків та тестування Telegram-бота.

### Етап 7: Повна SEO-оптимізація та тестування життєвого циклу замовлень
- [x] **SEO ЧПУ (Clean URLs):**
  - Реалізовано зрозумілі URL для категорій: `/category/{slug}` (наприклад `/category/made-in-ukraine`, `/category/animals`, `/category/anime` тощо).
  - Реалізовано пагінацію ЧПУ: `/category/{slug}/page/{n}` та `/page/{n}`.
  - Налаштовано правила `RewriteRule` у [`.htaccess`](file:///j:/site/SC/.htaccess) з захистом службових директорій.
  - Оновлено чіпи категорій у [`views/components/categories.php`](file:///j:/site/SC/views/components/categories.php) та функцію [`getCatalogPageUrl()`](file:///j:/site/SC/index.php).
- [x] **Динамічні SEO метадані та мікророзмітка ([`index.php`](file:///j:/site/SC/index.php), [`views/layout/header.php`](file:///j:/site/SC/views/layout/header.php)):**
  - Унікальні `<title>` та `<meta name="description">` під кожну категорію та сторінку.
  - Канонічні посилання (`<link rel="canonical">`) для запобігання дублюванню сторінок.
  - Повні OpenGraph та Twitter Card метатеги (`og:title`, `og:image`, `og:url`, `og:description`).
  - Schema.org JSON-LD структуровані дані (`Organization`, `WebSite` із пошуковою дією `SearchAction`).
- [x] **Карта сайту XML ([`sitemap.php`](file:///j:/site/SC/sitemap.php)):**
  - Динамічний генератор `sitemap.xml` з підтримкою Google XML Sitemap Protocol.
  - Включено головну сторінку, усі 22 категорії, сторінки каталогу та пагінації з пріоритетами та `lastmod`.
- [x] **Файл інструкцій для пошукових роботів ([`robots.txt`](file:///j:/site/SC/robots.txt)):**
  - Дозволено індексацію сайту (`Allow: /`) та закрито адмінку, api, cron і службові папки.
  - Вказано пряме посилання на `Sitemap: https://shopcase.top/sitemap.xml`.
- [x] **Тестування сповіщень та виправлення кодування UTF-8:**
  - Проведено діагностику та виправлено формування запитів у [`app/TelegramService.php`](file:///j:/site/SC/app/TelegramService.php): перехід на чистий JSON payload (`Content-Type: application/json; charset=utf-8`) замість `http_build_query`.
  - Реалізовано безпечне екранування символів `escapeHtml()` (`ENT_NOQUOTES | ENT_SUBSTITUTE, 'UTF-8'`), що запобігає перетворенню українських апострофів (`'`, `’`) та лапок (`"`) у нерозпізнані сутності HTML (`&#039;` / `&quot;`).
  - У [`app/Config.php`](file:///j:/site/SC/app/Config.php) встановлено глобальне внутрішнє кодування `mb_internal_encoding('UTF-8')`.
  - В адмін-панель ([`admin.php`](file:///j:/site/SC/admin.php)) додано окрему кнопку для тестування відправки повного тестового замовлення з українськими символами.

### Етап 8: Комплексне розширення функціоналу Адмін-панелі
- [x] **Керування цінами та націнками (Pricing & Margins Manager):**
  - Реалізовано динамічну систему цін [`app/Config.php`](file:///j:/site/SC/app/Config.php) (`getMaterialPrices()`, `saveMaterialPrices()`, `getMinPrice()`).
  - Створено інтерфейс редагування цін для всіх 8 матеріалів чохлів (Силікон, TPU, Bumper, MagSafe, 3D глянець/мат тощо) зі збереженням у `data/pricing.json`.
  - Впроваджено інтерактивний клієнтський калькулятор маржі та прибутку на основі дроп-собівартості EndorPhone.
  - На вітрині ([`index.php`](file:///j:/site/SC/index.php)) та в Hero-блоці ([`views/components/hero.php`](file:///j:/site/SC/views/components/hero.php)) ціни тепер підтягуються динамічно.
- [x] **Управління каталогом принтів (Designs Manager):**
  - Додано можливість завантаження нових принтів з файловим аплоадом (JPG, PNG, WebP) у відповідні директорії категорій `design/{category_slug}/`.
  - Модальне вікно редагування метаданих принта: зміна назви, категорії, пошукових тегів та бейджа «🔥 ТОП».
  - Функціонал видалення / приховування принтів з каталогу зі збереженням у `data/custom_designs.json`.
  - Автоматичне оновлення кешу [`app/Database.php`](file:///j:/site/SC/app/Database.php) та динамічної карти сайту [`sitemap.php`](file:///j:/site/SC/sitemap.php).
- [x] **Управління категоріями (Categories Manager):**
  - Перегляд списку категорій, кількості принтів та порядку сортування.
  - Додавання нових категорій (назва, slug, емодзі/іконка, сортування) та редагування існуючих.
  - Збереження у `data/custom_categories.json`.
- [x] **Управління та експорт замовлень (Orders Management & CSV Export):**
  - Жива фільтрація замовлень за 20 системними статусами EndorPhone та текстовий пошук за ПІБ, телефоном, містом, номером ТТН.
  - Модальне вікно повного редагування замовлення (статус, ТТН, контактні дані покупця, сума, адреса Нової Пошти, коментар).
  - Експорт замовлень у CSV файл із кодуванням UTF-8 BOM для бездоганного відкриття в Microsoft Excel.
- [x] **Аналітика та системні інструменти (Analytics & Tools):**
  - KPI-картки: загальний грошовий оборот, виконані замовлення, середній чек, конверсія викупу.
  - Аналітичний розподіл замовлень за статусами, маржинальністю та фінансами.
  - Інструменти для ручного оновлення залишків EndorPhone API Stock та скидання кешу.

### Етап 9: Маркетингова інфраструктура, рекламні пікселі, UTM-трекінг та E-Commerce аналітика
- [x] **Комплексний модуль аналітики ([`js/analytics.js`](file:///j:/site/SC/js/analytics.js)):**
  - Автоматичне захоплення та кешування UTM-міток (`utm_source`, `utm_medium`, `utm_campaign`, `utm_content`, `utm_term`, `gclid`, `fbclid`, `ttclid`) у `sessionStorage`, `localStorage` та cookie на 30 днів (збереження атрибуції першого та останнього входу).
  - Автоматичне визначення джерел за `document.referrer` (Instagram, TikTok, Facebook, Google, Telegram, YouTube).
  - Відстеження повного життєвого циклу e-commerce подій: `PageView`, `ViewContent` (`view_item`), `InitiateCheckout` (`begin_checkout`), `Purchase` (`purchase`), `ConstructorOpen`.
  - Уніфікована передача подій у Google Tag Manager (`dataLayer`), Google Analytics 4 (`gtag`), Meta Pixel (`fbq`) та TikTok Pixel (`ttq`).
- [x] **Інтеграція пікселів та GTM у шаблони:**
  - Оновлено [`views/layout/header.php`](file:///j:/site/SC/views/layout/header.php): динамічне підключення Google Tag Manager, Google Analytics 4, Meta Pixel та TikTok Pixel з підтримкою `<noscript>` fallback у [`views/layout/footer.php`](file:///j:/site/SC/views/layout/footer.php).
  - Інтегровано виклики `Analytics.trackViewContent` у [`js/customizer.js`](file:///j:/site/SC/js/customizer.js), `Analytics.trackInitiateCheckout` та `Analytics.trackPurchase` у [`js/order.js`](file:///j:/site/SC/js/order.js), `Analytics.trackConstructorOpen` у [`js/constructor.js`](file:///j:/site/SC/js/constructor.js).
- [x] **Збереження UTM-атрибуції замовлень:**
  - Оновлено [`app/OrderService.php`](file:///j:/site/SC/app/OrderService.php): санітизація та збереження повного об'єкта `utm` у записі кожного замовлення.
  - Оновлено [`app/TelegramService.php`](file:///j:/site/SC/app/TelegramService.php): сповіщення адміністратора в Telegram тепер містить акуратний блок рекламного джерела (джерело, канал, кампанія, креатив).
- [x] **Маркетинговий кабінет та аналітика в Адмін-панелі ([`admin.php`](file:///j:/site/SC/admin.php)):**
  - **Вкладка «🎯 Маркетинг & Пікселі»:** інтерфейс збереження ID пікселів (GTM, Meta, TikTok, GA4) зі статусами активності та таблицею e-commerce подій.
  - **Інтерактивний генератор UTM-посилань:** швидке створення валідних рекламних посилань для будь-якої сторінки чи категорії під Instagram, TikTok, Google, Facebook із кнопкою копіювання в 1 клік.
  - **Вкладка «📦 Замовлення»:** кольорові бейджі джерел трафіку в таблиці (`Instagram`, `TikTok`, `Google`, `Facebook`, `Telegram`, `Direct/Органіка`) та блок детальної UTM-інформації в модальному вікні замовлення.
  - **Експорт у CSV (Excel):** додано стовпчики `UTM Source`, `UTM Medium`, `UTM Campaign`, `UTM Content`, `UTM Term`, `Referrer`.
  - **Вкладка «📊 Аналітика»:** аналітичні звіти замовлень та виручки за джерелами трафіку (`utm_source`) та ТОП рекламними кампаніями (`utm_campaign`).
- [x] **Мобільна та десктопна оптимізація кастомізатора принтів ([`css/customizer.css`](file:///j:/site/SC/css/customizer.css), [`js/customizer.js`](file:///j:/site/SC/js/customizer.js)):**
  - **Пропорційний макет прев'ю:** повністю виправлено застарілий розмір 220×420px. На смартфонах прев'ю перетворено на компактну горизонтальну шапку (міні-макет 52×96px із назвою, категорією та чіпом матеріалу).
  - **Повна видимість форми та кнопки «Купити»:** форма вибору моделі телефону та матеріалів миттєво відображається під шапкою, а футер із сумою замовлення та кнопкою «Купити» надійно зафіксований внизу екрана (`position: sticky; bottom: 0;`).
  - **Очищення шапки сайту ([`views/layout/header.php`](file:///j:/site/SC/views/layout/header.php)):** прибрано дублюючі кнопки «Своє фото» та «Статус» із правого блоку шапки, залишивши чисту навігацію та мобільний бургер.
  - **Оновлення версій ассетів (`?v=3.3`):** оновлено версії кешування для миттєвого підвантаження нових стилів та скриптів.

---

## 🔮 Заплановані завдання / Ідеї для розвитку (Backlog 📋)
1. **Онлайн-оплата (LiqPay / Monobank / WayForPay):**
   - Підключення еквайрингу для миттєвої оплати карткою/Apple Pay/Google Pay.
2. **SMS / Viber сповіщення клієнту:**
   - Автоматична відправка номеру ТТН клієнту при відправці замовлення (TurboSMS / AlphaSMS).
3. **Промокоди та програми лояльності:**
   - Модуль купонів на знижку у формі оформлення замовлення.

